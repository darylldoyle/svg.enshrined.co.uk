# Everything generated on the host: the installed library trees and the manifest
# the site reads. Under a zero-downtime deploy this is the one shared directory.
/storage/*
!/storage/.gitkeep

.DS_Store
.idea/
.vscode/
